{% set name = "css_parser" %}
{% set version = "2.0.0" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  - url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
    sha256: af5c759a127b125b635006a6c6c2e05b96a1ebdeec21b3c415fd5f09ec714a0a

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies -n $PREFIX/bin {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem

requirements:
  host:
    - ruby
  run:
    - {{ pin_compatible('ruby', max_pin='x.x') }}
    - rb-addressable >=0

test:
  commands:
    - ruby -r css_parser -e 'exit 0'

about:
  home: https://rubygems.org/gems/css_parser
  license: MIT
  license_family: MIT
  license_file: MIT-LICENSE
  summary: A set of classes for parsing CSS in Ruby.

  doc_url: https://www.rubydoc.info/gems/css_parser
  dev_url: https://github.com/premailer/css_parser

extra:
  recipe-maintainers:
    - ajaypnair09
